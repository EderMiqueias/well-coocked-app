import { Instructions } from "@/types";

export const INSTRUCTIONS_ARRAY = Object.values(Instructions);
export const WAITING_TIMEOUT_SECONDS = 8;
export const COOKING_TIMEOUT_SECONDS = 8;

export const ANIMATION_TIME_CSS = '1.2s';
export const TIMEOUT_MS = 1000;
export const SHORT_TIMEOUT_MS = 400;
export const LONG_TIMEOUT_MS = 2000;
